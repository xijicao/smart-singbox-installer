name: self-check

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  shell-and-layout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run project self-check
        run: sh tools/self-check.sh
