# 树状图图片

![详细架构树状图](./详细架构树状图.svg)

# 架构思维导图

## Mermaid 版

```mermaid
mindmap
  root((一条命令走天下))
    唯一安装命令
      curl 或 wget 拉 GitHub install.sh
      进入固定菜单
        1 DMIT入口机
        2 HK入口机
        3 Home只装SS2022
        4 Home装Reality和SS2022
    入口机器
      DMIT
        Debian或Ubuntu
        2G带宽
        双栈
        主力Reality
        输出你的链接
        输出朋友链接
        可粘贴ss链接生成中转Reality
      HK
        Debian或Ubuntu
        10M小水管
        IPv4单栈
        国际互联中心
        输出你的链接
        输出朋友链接
        中转日本台湾美国欧洲落地
    Home落地机器
      选3
        只装SS2022
        适合直连一般的家宽NATLXCIPLC
        输出ss_link
        输出ss_link_editable
      选4
        装Reality直连
        装SS2022落地
        适合直连和性能都还行的机器
        两个协议不要共用端口
      系统识别
        Debian或Ubuntu用glibc包
        Alpine用musl包
        对应包失败再用通用包
    导入落地
      home机器
        cat /root/home-singbox-info.txt
        复制ss_link
        NAT映射时改ss_link_editable公网端口
      DMIT或HK
        运行sing-box-add-ss2022-relay
        粘贴ss链接
        自动生成vless中转Reality
    后续维护
      sb或-sb
        重新生成基础Reality链接
        列出relay节点
        删除relay节点
        查看当前所有Reality链接
        备份配置
        恢复最新备份
        只更新sing-box核心
      sing-box-uninstall
        只在home落地机器使用
        卸载前自动备份
    安全
      info文件600权限
      root才能读
      不上传真实密钥到GitHub
    GitHub自检
      tools/self-check.sh
      Actions自动检查语法和危险残留
```

## 文字版

### 1. 所有机器都只跑一条安装命令

```sh
curl -fsSL https://raw.githubusercontent.com/xijicao/smart-singbox-installer/main/install.sh | sh
```

如果没有 `curl`：

```sh
wget -qO- https://raw.githubusercontent.com/xijicao/smart-singbox-installer/main/install.sh | sh
```

然后按菜单选：

```text
1) DMIT entry machine
2) HK entry machine
3) Home/landing machine - SS2022 only
4) Home/landing machine - Reality + SS2022
```

### 2. DMIT 和 HK 是入口

`DMIT` 和 `HK` 不是落地机器，它们是你和朋友平时导入客户端的入口。

默认安装后都会输出两个 Reality 链接：

1. 你用的链接
2. 朋友用的链接

同时都会安装两个管理工具：

```sh
sing-box-add-ss2022-relay
sb
-sb
```

### 3. 家宽 / LXC / NAT / IPLC 都是 home 机器

不再给 IPLC 单独搞特殊脚本。

只想做落地，菜单选：

```text
3
```

性能好或直连好，菜单选：

```text
4
```

### 4. NAT/LXC 端口映射

如果是 NAT/LXC 端口映射，比如公网 `24496 -> 443`，home 机器实际监听还是 `443`。

安装后查看：

```sh
cat /root/home-singbox-info.txt
```

找到：

```text
ss_link_editable:
ss://method:password@server:443#jp-home-SS2022
```

把里面的 `:443` 改成 `:24496`，再粘到 HK/DMIT 的 `sing-box-add-ss2022-relay`。机器实际监听端口不用改。

如果菜单选 `4`，SS2022 和 Reality 不要共用同一个公网端口。推荐：

```text
24496 -> 443   # SS2022
24497 -> 8443  # Reality
```

### 5. 添加落地的流程

先在 home 机器安装，查看：

```sh
cat /root/home-singbox-info.txt
```

复制里面的：

```text
ss_link:
ss://...
```

再到 DMIT 或 HK 运行：

```sh
sing-box-add-ss2022-relay
```

粘贴 `ss://`，它会自动生成新的 Reality 中转链接。

### 6. 删除落地的流程

到 DMIT 或 HK 运行：

```sh
sb
```

选择：

```text
3) Delete a relay Reality node
```

然后按编号删除对应 `relay-*` 节点。

### 7. 查看和维护

到 DMIT 或 HK 运行：

```sh
sb
```

常用选项：

```text
1) Regenerate base Reality links for me/friend
4) Show all current Reality links
5) Backup config/meta now
6) Restore latest config/meta backup
7) Update sing-box core only
```

第 `7` 项只更新 sing-box 二进制，不改配置。更新前会自动备份，检查失败会恢复旧 core。

### 8. home 落地卸载

只在 home 落地机器上运行：

```sh
sing-box-uninstall
```

它会要求输入 `UNINSTALL`，并先打包备份。

### 9. GitHub 自检

上传仓库后会自动跑：

```sh
sh tools/self-check.sh
```

用来检查脚本语法、文件结构、旧危险文案残留和假值泄露。
